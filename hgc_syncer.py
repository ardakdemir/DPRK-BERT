import time
import os
import subprocess


hgc_root = "hgc:~"
class HgcSyncer:
    def __init__(self,root_folder,save_folder):
        self.root_folder = root_folder
        if not os.path.exists
    def sync(self):
        while True:
            dest = os.path.join(hgc_root,self.root_folder,"*")
            time.sleep(10)



